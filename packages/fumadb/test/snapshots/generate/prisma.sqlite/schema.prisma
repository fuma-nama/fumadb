model users {
  id String @id @default(cuid())
  name String
  email String
  image String? @default("my-avatar")
}

model accounts {
  id String @id
}
datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}